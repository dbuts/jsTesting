var width = 500;
var height = 500;

function setup() {
	createCanvas(500,500);
}

function draw() {
	background(0,120,120);
	stroke(125);
	fill(120);
	rect(30, 20, 55, 55);
}

function generateGrid(){
    for(i = 0; i < height; i++){
        for(j = 0; j < width; j++){
           }
    }
}
